# 지표 모니터링 및 경보 시스템

metric을 모니터링 하고 경보를 날려보자

데이터 수집, 전송, 저장소, 경보, 시각화로 구성

---

## 데이터 저장소로 무엇을 사용해야하나

InfluxDB 많이 들어봤다 근데 자세히는 몰랐음

InfluxDB는 시계열 데이터베이스(Time Series DB)

### 시계열 디비가 뭔데?

```
시계열 데이터를 처리하기 위해 최적화된 데이터베이스로써 빠르고 정확하게 실시간으로 쌓이는 대규모 데이터들을 처리할 수 있도록 고안
시간의 흐름에 따라 데이터를 분석하기에 매우 용이

특징
1. 시간을 기반으로 하는 저장 공간
2. 빠른 처리 속도
3. 다양한 자동화된 기능 제공

Trade-off
- INSERT와 SELECT에 최적화되어 DELETE나 UPDATE에 대한 기능이 상당히 제한된다.
- 시간에 따라 데이터를 오름차순으로 정렬하므로 임의의 시간으로 데이터를 읽고 쓰는 작업(Read/Write)은 성능이 떨어진다.
- 대량의 데이터가 쓰여지도록 최적화되어 DB에 부하가 걸린 경우 항상 최신의 데이터를 반환하지 못할 수 있다.
```

그렇다고 한다.

---

## 풀 vs 푸시 모델

더 나은 정답은 없다

**풀 모델**은

지표 수집기가 웹서버, db 클러스터, 큐 클러스터, 캐시 클러스터 같은 metric source에서 가져온다

"지표 수집기" 이 녀석이 서비스 탐색 서비스에서 지표 수집할 대상 수집하고 /metric 엔드포인트 날려서 가져옴

**푸시 모델**은 반대로 metric source들이 지표 수집기한데 metric을 날려줌

모니터링 대상 서버에 수집 에이전트를 설치하고 수집기에 전달

| 항목 | 풀 모델 | 푸시 모델 |
| --- | --- | --- |
| 손쉬운 디버깅 | 특정 엔드포인트를 두어 언제든 지표 데이터 조회 가능 → **풀 모델이 더 낫다** | - |
| 상태 진단 (health check) | 풀 요청에 응답하지 않으면 장애로 인지 가능 → **풀 모델이 더 쉽다** | **네트워크 장애와 서버 장애 중 어떤 게 원인인지 구분 어려움** |
| 생존 기간이 짧은 프로세스 | - | 수집기가 데이터를 끌어가기 전에 종료되어도 push gateway 사용으로 해결 가능 → **푸시 모델이 유리** |
| 방화벽/복잡한 네트워크 구성 | 동일한 엔드포인트 구성 가능, 하지만 멀티 데이터센터 환경에선 설계가 복잡함 | 수집기가 로드밸런서 뒤에 있으면 어디서든 수집 가능 → **푸시 모델이 더 낫다** |
| 성능 | TCP 사용 → 오버헤드가 낮고 안정적 | UDP 사용 가능 → 지표 전송 지연 낮음 |
| 데이터 신빙성 | 애플리케이션 서버가 명확 → 신뢰 가능 | 아무나 수집기에 보내기 쉬움 → 허용 목록 설정이나 인증 등 보완 필요 |
| 사례 | 프로메테우스 | Amazon CloudWatch, Graphite |

---

## 전송 및 저장

지표 수집기가 열심히 수집하고 나면 tsdb에 저장해야한다

근데 db에 장애 생기면 데이터 손실이 날테니까 이걸 막기 위해 사이에 kafka를 넣어보자

이제는 안다. 이렇게 하면 결합도가 낮아진다는 것을…

---

## 질의 서비스

질의 서비스는 질의 서비스 클러스터 형태로 만든다 쿼리 날리는 애도 분리하는 느낌?

시계열 데이터베이스에서는 sql말고 자기들 만의 쿼리를 쓴대

---

## 저장소 계층 최적화

저장소 계층에서 저장 용량 최적화는 데이터 인코딩 및 압축, 다운 샘플링을 하면 된다

cold storage는 잘 안쓰는 비활성 데이터 저장하는 곳, 돈 적게 들음

---

## 경보 시스템

경보 시스템은 이미 만들어진게 좋으니까 그거 쓰면 좋을 듯

경보 설정파일 yaml로 만들어 놓고, 캐시에서 가져오고, 관리자가 질의 서비스 호출하고, 경보 이벤트 생성하고 카프카에 주고, 소비자가 다양한 채널로 알림 날려주기

---

## 시각화 시스템

그라파나 짱
